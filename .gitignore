# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — NEVER commit secrets
.env
.env*.local
.env.local
.env.production

# database — student PII must never enter git
*.db
*.db-journal
/prisma/*.db
/data/

# uploaded template scans
/public/uploads/
/uploads/

# student data source — PII, keep out of git
PhD_students_data.xls
*.xls
*.xlsx

# sample diploma scans — the filled licence contains PII; templates are uploaded via the app
diploma_licence_example_filled.pdf
diploma_PhD_template_v1.pdf

# typescript
*.tsbuildinfo
